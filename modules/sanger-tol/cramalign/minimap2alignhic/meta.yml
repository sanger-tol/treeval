# yaml-language-server: $schema=https://raw.githubusercontent.com/sanger-tol/nf-core-modules/main/modules/meta-schema.json
name: "cramalign_minimap2alignhic"
description: |
  Aligns a subset of Hi-C reads from a CRAM file to a reference genome using minimap2,
    pipes the resulting alignments through filterfiveend.pl (included), samtools fixmate and samtools sort, outputting
    an alignment in BAM format.
keywords:
  - align
  - cram
  - chunk
tools:
  - samtools:
      description: |
        SAMtools is a set of utilities for interacting with and post-processing
        short DNA sequence read alignments in the SAM, BAM and CRAM formats, written by Heng Li.
        These files are generated as output by short read aligners like BWA.
      homepage: http://www.htslib.org/
      documentation: http://www.htslib.org/doc/samtools.html
      doi: 10.1093/bioinformatics/btp352
      licence: ["MIT"]
      identifier: biotools:samtools
  - minimap2:
      description: |
        A versatile pairwise aligner for genomic and spliced nucleotide sequences.
      homepage: https://github.com/lh3/minimap2
      documentation: https://github.com/lh3/minimap2#uguide
      licence: ["MIT"]
      identifier: ""
  - filter5end.pl:
      description: |
        Filter alignments
      homepage: https://github.com/ArimaGenomics/mapping_pipeline
      documentation: https://github.com/ArimaGenomics/mapping_pipeline
      licence: ["MIT"]
      identifier: ""
input:
  - - meta:
        type: map
        description: |
          Groovy Map containing sample information
          e.g. `[ id:'sample1' ]`
    - cram:
        type: file
        description: CRAM file containing sequences to align
        pattern: "*.cram"
        ontologies:
          - edam: "http://edamontology.org/format_3462" # CRAM
    - crai:
        type: file
        description: CRAM index file
        pattern: "*.crai"
        ontologies:
          - edam: "http://edamontology.org/format_3475" # TSV
    - rglines:
        type: list
        description: A list of strings, each a valid SAM-format @RG header line,
          to pass to the aligner
  - - meta2:
        type: map
        description: |
          Groovy Map containing sample information
          e.g. `[ id:'sample1' ]`
    - index:
        type: file
        description: minimap2 index of the reference to be aligned to
        ontologies: []
    - reference:
        type: file
        description: FASTA file containing reference for alignment
        pattern: "*.fasta(.gz)?"
        ontologies:
          - edam: http://edamontology.org/format_1929 # FASTA
  - - chunkn:
        type: integer
        description: Integer index of CRAM chunk to map
    - range:
        type: list
        description: Start and end indices defining CRAM slices to align
output:
  bam:
    - - meta:
          type: map
          description: |
            Groovy Map containing sample information
            e.g. `[ id:'sample1' ]`
      - "*.bam":
          type: file
          description: BAM file of mapped Hi-C sequences
          pattern: "*.bam"
          ontologies:
            - edam: "http://edamontology.org/format_2572" # BAM
  versions_minimap2:
    - - ${task.process}:
          type: string
          description: Name of the process
      - minimap2:
          type: string
          description: The name of the tool
      - minimap2 --version | sed "s/minimap2 //g":
          type: eval
          description: The expression to obtain the version of the tool
  versions_gawk:
    - - ${task.process}:
          type: string
          description: Name of the process
      - gawk:
          type: string
          description: The name of the tool
      - gawk --version | grep -o -E "[0-9]+(\.[0-9]+)+" | head -n1:
          type: eval
          description: The expression to obtain the version of the tool
  versions_filterfiveend:
    - - ${task.process}:
          type: string
          description: Name of the process
      - filter_five_end.pl:
          type: string
          description: The name of the tool
      - echo 1.0:
          type: eval
          description: The expression to obtain the version of the tool
  versions_samtools:
    - - ${task.process}:
          type: string
          description: Name of the process
      - samtools:
          type: string
          description: Name of the tool
      - samtools --version | head -1 | sed -e "s/samtools //":
          type: eval
          description: The expression to obtain the version of the tool
topics:
  versions:
    - - ${task.process}:
          type: string
          description: Name of the process
      - minimap2:
          type: string
          description: The name of the tool
      - minimap2 --version | sed "s/minimap2 //g":
          type: eval
          description: The expression to obtain the version of the tool
    - - ${task.process}:
          type: string
          description: Name of the process
      - gawk:
          type: string
          description: The name of the tool
      - gawk --version | grep -o -E "[0-9]+(\.[0-9]+)+" | head -n1:
          type: eval
          description: The expression to obtain the version of the tool
    - - ${task.process}:
          type: string
          description: Name of the process
      - filter_five_end.pl:
          type: string
          description: The name of the tool
      - echo 1.0:
          type: eval
          description: The expression to obtain the version of the tool
    - - ${task.process}:
          type: string
          description: Name of the process
      - samtools:
          type: string
          description: Name of the tool
      - samtools --version | head -1 | sed -e "s/samtools //":
          type: eval
          description: The expression to obtain the version of the tool
authors:
  - "@prototaxites"
maintainers:
  - "@prototaxites"
